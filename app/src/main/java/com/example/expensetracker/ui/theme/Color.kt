package com.example.expensetracker.ui.theme

import androidx.compose.ui.graphics.Color

// Brand palette — red theme
val Red80 = Color(0xFFEF9A9A)   // light red for dark scheme primary
val Red40 = Color(0xFFC62828)   // deep red for light scheme primary
val RedDark = Color(0xFF7F0000) // darkest red

val Pink80 = Color(0xFFF48FB1)
val Pink40 = Color(0xFFAD1457)

// Semantic colors (kept distinct so income is still obviously positive)
val IncomeGreen = Color(0xFF2E7D32)
val ExpenseRed = Color(0xFFC62828)

val LightBackground = Color(0xFFFFF8F8)
val DarkBackground = Color(0xFF140C0C)
