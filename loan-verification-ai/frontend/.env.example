# Frontend public runtime configuration.
# Copy to .env.local for development. Only NEXT_PUBLIC_* vars reach the browser.

# Base URL of the backend REST API (versioned).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1

# Product name shown in the UI and document titles.
NEXT_PUBLIC_APP_NAME=VerifyCo
